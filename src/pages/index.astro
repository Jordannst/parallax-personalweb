---
import BaseLayout from '../layouts/BaseLayout.astro';
import Preloader from '../components/ui/Preloader.jsx';
import Hero from '../components/sections/Hero.astro';
import About from '../components/sections/About.astro';
import ZoomSection from '../components/parallax/ZoomSection.jsx';
import StackedProjectScroll from '../components/parallax/StackedProjectScroll.jsx';
import TechStack from '../components/sections/TechStack.jsx';
import Contact from '../components/sections/Contact.astro';
import { projects } from '../data/projects';
---

<BaseLayout title="Jordan Sutarto | Creative Developer">
  
  <!-- Preloader -->
  <Preloader client:load />
  
  <!-- Hero Section with Deep Dive Parallax (sticky handled internally) -->
  <div class="relative z-0">
    <Hero />
  </div>
  
  <!-- About Section (z-10) - Paper Light, stacks over Hero -->
  <div class="relative z-10 shadow-[0_-10px_40px_-15px_rgba(0,0,0,0.5)]">
    <About />
  </div>
  
  <!-- Zoom Portal Transition (z-20) -->
  <div class="relative z-20">
    <ZoomSection client:load />
  </div>
  
  <!-- Projects Section (z-30) - Stacked Cards Layout -->
  <div class="relative z-30">
    <StackedProjectScroll client:load projects={projects} />
  </div>
  
  <!-- TechStack Section (z-40) - Paper Light, shape-shifting animation -->
  <div class="sticky top-0 z-40">
    <TechStack client:visible />
  </div>
  
  <!-- Contact Section (z-50) - The Grand Finale -->
  <div class="relative z-50">
    <Contact />
  </div>

</BaseLayout>
